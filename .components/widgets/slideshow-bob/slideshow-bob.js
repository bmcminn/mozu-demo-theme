define([
  'modules/jquery-mozu'
, 'vendor/slick'
], function($) {

    var temp
      , regex = {
        }
      , $slideshows   = $('[data-mz-widget="bcm~slideshow-bob"]')
      , $slides       = $('[data-mz-widget="bcm~slideshow-mel"]')
      ;


    console.debug('slideshows', $slideshows);
    console.debug('slides',     $slides);

    // need to finish adding functionality here


    // $slideshow.slick();
});
